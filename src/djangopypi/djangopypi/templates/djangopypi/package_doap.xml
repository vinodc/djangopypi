<?xml version="1.0" encoding="UTF-8" ?>
<rdf:RDF
	xmlns="http://usefulinc.com/ns/doap#"
	xmlns:foaf="http://xmlns.com/foaf/0.1/"
	xmlns:rdf="http://www.w3.org/1999/02/22-rdf-syntax-ns#">
	<Project>
		<name>{{ package.name }}</name>
		{% if package.latest %}
		{% with package.latest as release %}
		<shortdesc>{{ release.summary }}</shortdesc>
		{% if release.description %}
		<description>
			{{ release.description }}
		</description>
		{% endif %}
		{% if release.package_info.home_page %}
		<homepage rdf:resource="{{ release.package_info.home_page }}" />
		{% endif %}
		{% if release.package_info.download_url %}
		<download-page rdf:resource="{{ release.package_info.download_url }}" />
		{% endif %}
		{% if release.package_info.author %}
		<developer>
			<foaf:Person>
				<foaf:name>{{ release.package_info.author }}</foaf:name>
				{% if release.package_info.author_email %}
				<foaf:mbox>{{ release.package_info.author_email }}</foaf:mbox>
				{% endif %}
			</foaf:Person>
		</developer>
		{% endif %}
		{% if release.package_info.maintainer %}
		<maintainer>
			<foaf:Person>
				<foaf:name>{{ release.package_info.maintainer }}</foaf:name>
				{% if release.package_info.maintainer_email %}
				<foaf:mbox>{{ release.package_info.maintainer_email }}</foaf:mbox>
				{% endif %}
			</foaf:Person>
		</maintainer>
		{% endif %}
		{% if release.package_info.license %}
		<license>{{ release.package_info.license }}</license>
		{% endif %}
		{% endwith %}
		{% endif %}
		{% for release in package.releases.all %}
		<release rdf:about="{{ release.get_absolute_url }}">
			<Version>
				<revision>{{ release.version }}</revision>
			</Version>
		</release>
		{% endfor %}
	</Project>
</rdf:RDF>